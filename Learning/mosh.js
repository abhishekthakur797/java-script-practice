// Variable
let name = "abhishek";
console.log(name);

// For Fixed values use const it can't be changed
const interestRate = 0.5;

// Primitive types
let age = 34;
let isApproved = true;

// Undefined and null : by default if you can define a variable it will be undefined but your
// can write also
let firstName = undefined;
let lastName = null;

// how to check what type of function
// typeof isApproved;
alert("this is a an alert");
